name: Workflow liés

on:
  workflow_dispatch:

jobs:
  job_1:
    name: Job 1 - Lancement manuel
    runs-on: ubuntu-latest

    steps:
      - name: Message du job 1
        run: echo "✅ Le job 1 a été exécuté manuellement."

  job_2:
    name: Job 2 - Après le job 1
    runs-on: ubuntu-latest
    needs: job_1

    steps:
      - name: Message du job 2
        run: echo "🚀 Le job 2 s'exécute après le job 1."
